---
import { tagStyle, type TagColor } from '../tag.config.js';

interface Props {
	tag: {
		label: string;
		color?: TagColor;
	};
}

const { tag } = Astro.props;
---

<span class="tag-chip" style={tagStyle(tag.color)}>
	{tag.label}
</span>
